  define([
    'jquery',
    'underscore',
    'backbone',
], function($, _, Backbone) {
    var ProfileAttribute = Backbone.Model.extend({
        initialize: function() {

        }
    });

    return ProfileAttribute;
});
