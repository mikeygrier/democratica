 <h1 class="form-signin-heading"><%= $heading_title %></h1>
 <div class="form-signin">
 <% if ($message) { %> 
   <div class="form-signin-message lead muted">
     <%= $message %>
   </div>
 <% } %>
 <a href="<%= M$back %>" class="btn btn-lg btn-primary btn-block" role="button">OK</a>
 </div>
